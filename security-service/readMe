Key generation threw openSSL:
https://www.golinuxcloud.com/openssl-cheatsheet/
openssl genrsa -out keypair.pem 2048
openssl rsa -in keypair.pem -pubout -out pubkey.pem
openssl pkcs8 -topk8 -inform PEM -nocrypt -in keypair.pem -out private.pem
